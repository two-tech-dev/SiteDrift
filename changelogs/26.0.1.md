## 26.0.1

### Fixed
- `before-navigate` page crash when extension options are not yet set
- Helpers import in injection script — `insertInfoBox` and other helpers now resolve correctly
- Duplicate `lnk2cc.ts` bypass removed (conflicted with `lnk2.ts`)
- Spaste bypass updated for hCaptcha (old custom captcha logic removed)
- Spaste match pattern fixed (`www.spaste.com` instead of path-based match)

### Added
- Adfocus bypass — reads `click_url` from page source, skips countdown
- Chrome Web Store and Firefox Add-ons install links in README

### Changed
- Website redesigned with light theme, removing AI-generic patterns (rainbow text, glassmorphism, glow effects)
- lnk2.cc bypass improved — enables button immediately, auto-submits on `/go/` page
